[workspace]
resolver = "2"
members = [
    "core",
    "ast",
    "hir",
    "mir",
    "interpreter",
    "rt/c",
    "runtime",
    "llvm",
    "compiler"]

exclude = [
    "stdlib-macros",
    "stdlib"]

default-members = ["compiler", "interpreter"]