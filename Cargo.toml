[workspace]
resolver = "2"
members = [
    "core",
    "ast",
    "hir",
    "mir",
    "interpreter",
    #"rt/rust",
    "rt/c",
    "llvm",
    "compiler"]

exclude = [
    "rt/rust"]

default-members = ["compiler", "interpreter"]