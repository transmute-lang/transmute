
#fixme removing the print() call makes the function return a number. this is wrong because the fact that the
  # signature is void must override it.
#fixme comments fuckup lines/columns
let main() {
    let a = [0];
    print(a[1]);
}